> from abc import ABCMeta, abstractmethod
> from typing import Generator, Optional
  
  
> class LLMClientPort(metaclass=ABCMeta):
>     @abstractmethod
>     def transform_to_inferra_rules_stream(self, file_name: str, markdown_content: str) -> Generator[str, None, None]:
!         pass
  
>     @abstractmethod
>     def convert_file_to_markdown(self, file_path: str, file_type: str) -> str:
!         pass
